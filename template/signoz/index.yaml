apiVersion: app.sealos.io/v1
kind: Template
metadata:
  name: signoz
spec:
  title: 'SigNoz'
  url: 'https://signoz.io/'
  gitRepo: 'https://github.com/SigNoz/signoz'
  author: 'sealos'
  description: 'SigNoz is an open-source OpenTelemetry-native observability platform for logs, traces, and metrics.'
  readme: 'https://raw.githubusercontent.com/labring-actions/templates/kb-0.9/template/signoz/README.md'
  icon: 'https://raw.githubusercontent.com/labring-actions/templates/kb-0.9/template/signoz/logo.svg'
  screenshots:
    - 'https://raw.githubusercontent.com/labring-actions/templates/kb-0.9/template/signoz/website-screenshot.webp'
  templateType: inline
  categories:
    - tool
    - monitor
  locale: en
  defaults:
    app_name:
      type: string
      value: signoz-${{ random(8) }}
    app_host:
      type: string
      value: signoz-${{ random(8) }}
    tokenizer_jwt_secret:
      type: string
      value: ${{ random(32) }}
  i18n:
    zh:
      description: 'SigNoz 是一款基于 OpenTelemetry 的开源可观测平台，可统一管理日志、链路追踪和指标。'
      readme: 'https://raw.githubusercontent.com/labring-actions/templates/kb-0.9/template/signoz/README_zh.md'
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ${{ defaults.app_name }}-clickhouse-0
  labels:
    app: ${{ defaults.app_name }}-clickhouse-0
    cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}-clickhouse-0
data:
  vn-etcvn-clickhousevn-servervn-configvn-xml: |
    <?xml version="1.0"?>
    <clickhouse>
        <logger>
            <level>information</level>
            <formatting>
                <type>json</type>
            </formatting>
            <log>/var/log/clickhouse-server/clickhouse-server.log</log>
            <errorlog>/var/log/clickhouse-server/clickhouse-server.err.log</errorlog>
            <size>100M</size>
            <count>3</count>
        </logger>
        <http_port>8123</http_port>
        <tcp_port>9000</tcp_port>
        <mysql_port>9004</mysql_port>
        <postgresql_port>9005</postgresql_port>
        <interserver_http_host>${{ defaults.app_name }}-clickhouse-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local</interserver_http_host>
        <interserver_http_port>9009</interserver_http_port>
        <max_connections>4096</max_connections>
        <keep_alive_timeout>3</keep_alive_timeout>
        <max_concurrent_queries>100</max_concurrent_queries>
        <max_server_memory_usage>0</max_server_memory_usage>
        <max_thread_pool_size>10000</max_thread_pool_size>
        <max_server_memory_usage_to_ram_ratio>0.9</max_server_memory_usage_to_ram_ratio>
        <total_memory_profiler_step>4194304</total_memory_profiler_step>
        <total_memory_tracker_sample_probability>0</total_memory_tracker_sample_probability>
        <uncompressed_cache_size>8589934592</uncompressed_cache_size>
        <!-- https://clickhouse.com/docs/operations/tips -->
        <mark_cache_size>536870912</mark_cache_size>
        <mmap_cache_size>1000</mmap_cache_size>
        <compiled_expression_cache_size>134217728</compiled_expression_cache_size>
        <compiled_expression_cache_elements_size>10000</compiled_expression_cache_elements_size>
        <path>/var/lib/clickhouse/</path>
        <tmp_path>/var/lib/clickhouse/tmp/</tmp_path>
        <user_files_path>/var/lib/clickhouse/user_files/</user_files_path>
        <user_directories>
            <users_xml>
                <path>users.xml</path>
            </users_xml>
            <local_directory>
                <path>/var/lib/clickhouse/access/</path>
            </local_directory>
        </user_directories>
        <default_profile>default</default_profile>
        <custom_settings_prefixes></custom_settings_prefixes>
        <default_database>default</default_database>
        <mlock_executable>true</mlock_executable>
        <remap_executable>false</remap_executable>
        <macros>
            <shard>01</shard>
            <replica>example01-01-1</replica>
        </macros>
        <builtin_dictionaries_reload_interval>3600</builtin_dictionaries_reload_interval>
        <max_session_timeout>3600</max_session_timeout>
        <default_session_timeout>60</default_session_timeout>
        <query_log>
            <database>system</database>
            <table>query_log</table>
            <partition_by>toYYYYMM(event_date)</partition_by>
            <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        </query_log>
        <trace_log>
            <database>system</database>
            <table>trace_log</table>
            <partition_by>toYYYYMM(event_date)</partition_by>
            <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        </trace_log>
        <query_thread_log>
            <database>system</database>
            <table>query_thread_log</table>
            <partition_by>toYYYYMM(event_date)</partition_by>
            <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        </query_thread_log>
        <query_views_log>
            <database>system</database>
            <table>query_views_log</table>
            <partition_by>toYYYYMM(event_date)</partition_by>
            <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        </query_views_log>
        <part_log>
            <database>system</database>
            <table>part_log</table>
            <partition_by>toYYYYMM(event_date)</partition_by>
            <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        </part_log>
        <metric_log>
            <database>system</database>
            <table>metric_log</table>
            <flush_interval_milliseconds>7500</flush_interval_milliseconds>
            <collect_interval_milliseconds>1000</collect_interval_milliseconds>
        </metric_log>
        <asynchronous_metric_log>
            <database>system</database>
            <table>asynchronous_metric_log</table>
            <flush_interval_milliseconds>7000</flush_interval_milliseconds>
        </asynchronous_metric_log>
        <opentelemetry_span_log>
            <engine>
                engine MergeTree
                partition by toYYYYMM(finish_date)
                order by (finish_date, finish_time_us, trace_id)
            </engine>
            <database>system</database>
            <table>opentelemetry_span_log</table>
            <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        </opentelemetry_span_log>
        <crash_log>
            <database>system</database>
            <table>crash_log</table>
            <partition_by />
            <flush_interval_milliseconds>1000</flush_interval_milliseconds>
        </crash_log>
        <processors_profile_log>
            <database>system</database>
            <table>processors_profile_log</table>
            <partition_by>toYYYYMM(event_date)</partition_by>
            <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        </processors_profile_log>
        <dictionaries_config>*_dictionary.xml</dictionaries_config>
        <user_defined_executable_functions_config>*function.xml</user_defined_executable_functions_config>
        <user_scripts_path>/var/lib/clickhouse/user_scripts/</user_scripts_path>
        <distributed_ddl>
          <path>/clickhouse/task_queue/ddl</path>
        </distributed_ddl>
        <graphite_rollup_example>
            <pattern>
                <regexp>click_cost</regexp>
                <function>any</function>
                <retention>
                    <age>0</age>
                    <precision>3600</precision>
                </retention>
                <retention>
                    <age>86400</age>
                    <precision>60</precision>
                </retention>
            </pattern>
            <default>
                <function>max</function>
                <retention>
                    <age>0</age>
                    <precision>60</precision>
                </retention>
                <retention>
                    <age>3600</age>
                    <precision>300</precision>
                </retention>
                <retention>
                    <age>86400</age>
                    <precision>3600</precision>
                </retention>
            </default>
        </graphite_rollup_example>
        <format_schema_path>/var/lib/clickhouse/format_schemas/</format_schema_path>
        <query_masking_rules>
            <rule>
                <name>hide encrypt/decrypt arguments</name>
                <regexp>((?:aes_)?(?:encrypt|decrypt)(?:_mysql)?)\s*\(\s*(?:'(?:\\'|.)+'|.*?)\s*\)</regexp>
                <replace>\1(???)</replace>
            </rule>
        </query_masking_rules>
        <merge_tree_metadata_cache>
            <lru_cache_size>268435456</lru_cache_size>
            <continue_if_corrupted>true</continue_if_corrupted>
        </merge_tree_metadata_cache>
    </clickhouse>
  vn-etcvn-clickhousevn-servervn-usersvn-xml: |
    <?xml version="1.0"?>
    <clickhouse>
        <profiles>
            <default>
                <max_memory_usage>10000000000</max_memory_usage>
                <load_balancing>random</load_balancing>
            </default>
            <readonly>
              <readonly>1</readonly>
            </readonly>
        </profiles>
        <users>
            <default>
                <password></password>
                <profile>default</profile>
                <networks>
                    <ip>::/0</ip>
                </networks>
                <quota>default</quota>
            </default>
        </users>
        <quotas>
            <default>
                <interval>
                    <duration>3600</duration>
                    <queries>0</queries>
                    <errors>0</errors>
                    <result_rows>0</result_rows>
                    <read_rows>0</read_rows>
                    <execution_time>0</execution_time>
                </interval>
            </default>
        </quotas>
    </clickhouse>
  vn-etcvn-clickhousevn-servervn-customvn-functionvn-xml: |
    <functions>
        <function>
            <type>executable</type>
            <name>histogramQuantile</name>
            <return_type>Float64</return_type>
            <argument>
                <type>Array(Float64)</type>
                <name>buckets</name>
            </argument>
            <argument>
                <type>Array(Float64)</type>
                <name>counts</name>
            </argument>
            <argument>
                <type>Float64</type>
                <name>quantile</name>
            </argument>
            <format>CSV</format>
            <command>./histogramQuantile</command>
        </function>
    </functions>
  vn-etcvn-clickhousevn-servervn-configvn-dvn-clustervn-xml: |
    <?xml version="1.0"?>
    <clickhouse>
        <zookeeper>
            <node index="1">
                <host>${{ defaults.app_name }}-zookeeper-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local</host>
                <port>2181</port>
            </node>
        </zookeeper>
        <remote_servers>
            <cluster>
                <shard>
                    <replica>
                        <host>localhost</host>
                        <port>9000</port>
                    </replica>
                </shard>
            </cluster>
        </remote_servers>
    </clickhouse>
  vn-etcvn-clickhousevn-servervn-configvn-dvn-dockervn-relatedvn-configvn-xml: |
    <clickhouse>
        <listen_host>0.0.0.0</listen_host>
        <listen_try>1</listen_try>
    </clickhouse>

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ${{ defaults.app_name }}-otel-collector
  labels:
    app: ${{ defaults.app_name }}-otel-collector
    cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}-otel-collector
data:
  vn-etcvn-otelvn-collectorvn-configvn-yaml: |
    connectors:
      signozmeter:
        metrics_flush_interval: 1h
        dimensions:
          - name: service.name
          - name: deployment.environment
          - name: host.name
    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
          http:
            endpoint: 0.0.0.0:4318
      prometheus:
        config:
          global:
            scrape_interval: 60s
          scrape_configs:
            - job_name: otel-collector
              static_configs:
              - targets:
                - localhost:8888
                labels:
                  job_name: otel-collector
    processors:
      batch:
        send_batch_size: 10000
        send_batch_max_size: 11000
        timeout: 10s
      batch/meter:
        send_batch_max_size: 25000
        send_batch_size: 20000
        timeout: 1s
      resourcedetection:
        # Using OTEL_RESOURCE_ATTRIBUTES envvar, env detector adds custom labels.
        detectors: [env, system]
        timeout: 2s
      signozspanmetrics/delta:
        metrics_exporter: signozclickhousemetrics
        metrics_flush_interval: 60s
        latency_histogram_buckets: [100us, 1ms, 2ms, 6ms, 10ms, 50ms, 100ms, 250ms, 500ms, 1000ms, 1400ms, 2000ms, 5s, 10s, 20s, 40s, 60s ]
        dimensions_cache_size: 100000
        aggregation_temporality: AGGREGATION_TEMPORALITY_DELTA
        enable_exp_histogram: true
        dimensions:
          - name: service.namespace
            default: default
          - name: deployment.environment
            default: default
          # This is added to ensure the uniqueness of the timeseries
          # Otherwise, identical timeseries produced by multiple replicas of
          # collectors result in incorrect APM metrics
          - name: signoz.collector.id
          - name: service.version
          - name: browser.platform
          - name: browser.mobile
          - name: k8s.cluster.name
          - name: k8s.node.name
          - name: k8s.namespace.name
          - name: host.name
          - name: host.type
          - name: container.name
    extensions:
      health_check:
        endpoint: 0.0.0.0:13133
      pprof:
        endpoint: 0.0.0.0:1777
    exporters:
      clickhousetraces:
        datasource: tcp://${{ defaults.app_name }}-clickhouse-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local:9000/signoz_traces
        low_cardinal_exception_grouping: ${env:LOW_CARDINAL_EXCEPTION_GROUPING}
        use_new_schema: true
      signozclickhousemetrics:
        dsn: tcp://${{ defaults.app_name }}-clickhouse-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local:9000/signoz_metrics
      clickhouselogsexporter:
        dsn: tcp://${{ defaults.app_name }}-clickhouse-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local:9000/signoz_logs
        timeout: 10s
        use_new_schema: true
      signozclickhousemeter:
        dsn: tcp://${{ defaults.app_name }}-clickhouse-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local:9000/signoz_meter
        timeout: 45s
        sending_queue:
          enabled: false
      metadataexporter:
        cache:
          provider: in_memory
        dsn: tcp://${{ defaults.app_name }}-clickhouse-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local:9000/signoz_metadata
        enabled: true
        timeout: 45s
    service:
      telemetry:
        logs:
          encoding: json
      extensions:
        - health_check
        - pprof
      pipelines:
        traces:
          receivers: [otlp]
          processors: [signozspanmetrics/delta, batch]
          exporters: [clickhousetraces, metadataexporter, signozmeter]
        metrics:
          receivers: [otlp]
          processors: [batch]
          exporters: [signozclickhousemetrics, metadataexporter, signozmeter]
        metrics/prometheus:
          receivers: [prometheus]
          processors: [batch]
          exporters: [signozclickhousemetrics, metadataexporter, signozmeter]
        logs:
          receivers: [otlp]
          processors: [batch]
          exporters: [clickhouselogsexporter, metadataexporter, signozmeter]
        metrics/meter:
          receivers: [signozmeter]
          processors: [batch/meter]
          exporters: [signozclickhousemeter]
  vn-etcvn-managervn-configvn-yaml: |
    server_endpoint: ws://${{ defaults.app_name }}.${{ SEALOS_NAMESPACE }}.svc.cluster.local:4320/v1/opamp

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ${{ defaults.app_name }}-zookeeper-0
  annotations:
    originImageName: signoz/zookeeper:3.7.1
    deploy.cloud.sealos.io/minReplicas: '1'
    deploy.cloud.sealos.io/maxReplicas: '1'
  labels:
    cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}-zookeeper-0
    app: ${{ defaults.app_name }}-zookeeper-0
spec:
  replicas: 1
  revisionHistoryLimit: 1
  serviceName: ${{ defaults.app_name }}-zookeeper-0
  selector:
    matchLabels:
      app: ${{ defaults.app_name }}-zookeeper-0
  template:
    metadata:
      annotations:
        signoz.io/scrape: "true"
        signoz.io/port: "9141"
        signoz.io/path: "/metrics"
      labels:
        app: ${{ defaults.app_name }}-zookeeper-0
    spec:
      terminationGracePeriodSeconds: 10
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 0
        fsGroup: 1001
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: ${{ defaults.app_name }}-zookeeper-0
        image: signoz/zookeeper:3.7.1
        imagePullPolicy: IfNotPresent
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        ports:
        - containerPort: 2181
          name: client
        - containerPort: 2888
          name: server
        - containerPort: 3888
          name: leader-election
        - containerPort: 9141
          name: metrics
        env:
        - name: ZOO_SERVER_ID
          value: "1"
        - name: ALLOW_ANONYMOUS_LOGIN
          value: "yes"
        - name: ZOO_AUTOPURGE_INTERVAL
          value: "1"
        - name: ZOO_ENABLE_PROMETHEUS_METRICS
          value: "yes"
        - name: ZOO_PROMETHEUS_METRICS_PORT_NUMBER
          value: "9141"
        volumeMounts:
        - name: vn-bitnamivn-zookeeper
          mountPath: /bitnami/zookeeper
        resources:
          limits:
            cpu: 100m
            memory: 512Mi
          requests:
            cpu: 10m
            memory: 51Mi
        startupProbe:
          exec:
            command:
              - /bin/sh
              - -c
              - curl -s -m 2 http://localhost:8080/commands/ruok | grep error | grep null
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 24
        livenessProbe:
          exec:
            command:
              - /bin/sh
              - -c
              - curl -s -m 2 http://localhost:8080/commands/ruok | grep error | grep null
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command:
              - /bin/sh
              - -c
              - curl -s -m 2 http://localhost:8080/commands/ruok | grep error | grep null
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
  volumeClaimTemplates:
  - metadata:
      labels:
        app: ${{ defaults.app_name }}-zookeeper-0
        cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}-zookeeper-0
      annotations:
        path: /bitnami/zookeeper
        value: '1'
      name: vn-bitnamivn-zookeeper
    spec:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 1Gi

---
apiVersion: v1
kind: Service
metadata:
  name: ${{ defaults.app_name }}-zookeeper-0
  labels:
    cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}-zookeeper-0
    app: ${{ defaults.app_name }}-zookeeper-0
spec:
  ports:
  - port: 2181
    name: client
  - port: 2888
    name: server
  - port: 3888
    name: leader-election
  - port: 9141
    name: metrics
  - port: 8080
    name: admin
  selector:
    app: ${{ defaults.app_name }}-zookeeper-0

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ${{ defaults.app_name }}-clickhouse-0
  annotations:
    originImageName: clickhouse/clickhouse-server:25.5.6
    deploy.cloud.sealos.io/minReplicas: '1'
    deploy.cloud.sealos.io/maxReplicas: '1'
  labels:
    cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}-clickhouse-0
    app: ${{ defaults.app_name }}-clickhouse-0
spec:
  replicas: 1
  revisionHistoryLimit: 1
  serviceName: ${{ defaults.app_name }}-clickhouse-0
  selector:
    matchLabels:
      app: ${{ defaults.app_name }}-clickhouse-0
  template:
    metadata:
      annotations:
        signoz.io/scrape: "true"
        signoz.io/port: "9363"
        signoz.io/path: "/metrics"
      labels:
        app: ${{ defaults.app_name }}-clickhouse-0
    spec:
      automountServiceAccountToken: false
      initContainers:
      - name: init-clickhouse
        image: clickhouse/clickhouse-server:25.5.6
        imagePullPolicy: IfNotPresent
        command:
        - bash
        - -c
        - |
          version="v0.0.1"
          node_os=$$(uname -s | tr '[:upper:]' '[:lower:]')
          node_arch=$$(uname -m | sed s/aarch64/arm64/ | sed s/x86_64/amd64/)
          echo "Fetching histogram-binary for $${node_os}/$${node_arch}"
          cd /tmp
          wget -O histogram-quantile.tar.gz "https://github.com/SigNoz/signoz/releases/download/histogram-quantile%2F$${version}/histogram-quantile_$${node_os}_$${node_arch}.tar.gz"
          tar -xvzf histogram-quantile.tar.gz
          mkdir -p /var/lib/clickhouse/user_scripts
          mv histogram-quantile /var/lib/clickhouse/user_scripts/histogramQuantile
        volumeMounts:
        - name: vn-varvn-libvn-clickhouse
          mountPath: /var/lib/clickhouse
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 12Mi
      - name: wait-for-zookeeper
        image: busybox:1.36.1
        imagePullPolicy: IfNotPresent
        command: ['sh', '-c', 'until wget -qO- http://${{ defaults.app_name }}-zookeeper-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local:8080/commands/ruok | grep error | grep null; do echo waiting for zookeeper; sleep 2; done;']
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 12Mi
      containers:
      - name: ${{ defaults.app_name }}-clickhouse-0
        image: clickhouse/clickhouse-server:25.5.6
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8123
          name: http
        - containerPort: 9000
          name: tcp
        - containerPort: 9009
          name: interserver
        - containerPort: 9363
          name: metrics
        volumeMounts:
        - name: ${{ defaults.app_name }}-clickhouse-0-cm
          mountPath: /etc/clickhouse-server/config.xml
          subPath: vn-etcvn-clickhousevn-servervn-configvn-xml
        - name: ${{ defaults.app_name }}-clickhouse-0-cm
          mountPath: /etc/clickhouse-server/users.xml
          subPath: vn-etcvn-clickhousevn-servervn-usersvn-xml
        - name: ${{ defaults.app_name }}-clickhouse-0-cm
          mountPath: /etc/clickhouse-server/custom-function.xml
          subPath: vn-etcvn-clickhousevn-servervn-customvn-functionvn-xml
        - name: ${{ defaults.app_name }}-clickhouse-0-cm
          mountPath: /etc/clickhouse-server/config.d/cluster.xml
          subPath: vn-etcvn-clickhousevn-servervn-configvn-dvn-clustervn-xml
        - name: ${{ defaults.app_name }}-clickhouse-0-cm
          mountPath: /etc/clickhouse-server/config.d/docker_related_config.xml
          subPath: vn-etcvn-clickhousevn-servervn-configvn-dvn-dockervn-relatedvn-configvn-xml
        - name: vn-varvn-libvn-clickhouse
          mountPath: /var/lib/clickhouse
        - name: vn-varvn-logvn-clickhousevn-server
          mountPath: /var/log/clickhouse-server
        env:
        - name: CLICKHOUSE_SKIP_USER_SETUP
          value: "1"
        resources:
          limits:
            cpu: 100m
            memory: 1024Mi
          requests:
            cpu: 10m
            memory: 102Mi
        startupProbe:
          httpGet:
            path: /ping
            port: 8123
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 36
        livenessProbe:
          httpGet:
            path: /ping
            port: 8123
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ping
            port: 8123
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
      volumes:
      - name: ${{ defaults.app_name }}-clickhouse-0-cm
        configMap:
          name: ${{ defaults.app_name }}-clickhouse-0
  volumeClaimTemplates:
  - metadata:
      labels:
        app: ${{ defaults.app_name }}-clickhouse-0
        cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}-clickhouse-0
      name: vn-varvn-libvn-clickhouse
      annotations:
        path: /var/lib/clickhouse
        value: '1'
    spec:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 1Gi
  - metadata:
      labels:
        app: ${{ defaults.app_name }}-clickhouse-0
        cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}-clickhouse-0
      name: vn-varvn-logvn-clickhousevn-server
      annotations:
        path: /var/log/clickhouse-server
        value: '1'
    spec:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 1Gi

---
apiVersion: v1
kind: Service
metadata:
  name: ${{ defaults.app_name }}-clickhouse-0
  labels:
    cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}-clickhouse-0
    app: ${{ defaults.app_name }}-clickhouse-0
spec:
  ports:
  - port: 8123
    name: http
  - port: 9000
    name: tcp
  - port: 9009
    name: interserver
  - port: 9363
    name: metrics
  selector:
    app: ${{ defaults.app_name }}-clickhouse-0

---
apiVersion: batch/v1
kind: Job
metadata:
  name: ${{ defaults.app_name }}-telemetrystore-migrator
spec:
  backoffLimit: 3
  ttlSecondsAfterFinished: 300
  template:
    spec:
      automountServiceAccountToken: false
      initContainers:
        - name: wait-for-clickhouse
          image: busybox:1.36.1
          imagePullPolicy: IfNotPresent
          command: ['sh', '-c', 'until wget -qO- http://${{ defaults.app_name }}-clickhouse-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local:8123/ping | grep Ok; do echo waiting for clickhouse; sleep 2; done;']
          resources:
            limits:
              cpu: 100m
              memory: 128Mi
            requests:
              cpu: 10m
              memory: 12Mi
      containers:
        - name: ${{ defaults.app_name }}-telemetrystore-migrator
          image: signoz/signoz-otel-collector:v0.144.2
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -c
            - |
              /signoz-otel-collector migrate ready &&
              /signoz-otel-collector migrate bootstrap &&
              /signoz-otel-collector migrate sync up &&
              /signoz-otel-collector migrate async up
          env:
            - name: SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_DSN
              value: tcp://${{ defaults.app_name }}-clickhouse-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local:9000
            - name: SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_CLUSTER
              value: cluster
            - name: SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_REPLICATION
              value: "true"
            - name: SIGNOZ_OTEL_COLLECTOR_TIMEOUT
              value: 10m
          resources:
            limits:
              cpu: 100m
              memory: 128Mi
            requests:
              cpu: 10m
              memory: 12Mi
      restartPolicy: OnFailure

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ${{ defaults.app_name }}
  annotations:
    originImageName: signoz/signoz:v0.117.0
    deploy.cloud.sealos.io/minReplicas: '1'
    deploy.cloud.sealos.io/maxReplicas: '1'
  labels:
    cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}
    app: ${{ defaults.app_name }}
spec:
  replicas: 1
  revisionHistoryLimit: 1
  serviceName: ${{ defaults.app_name }}
  selector:
    matchLabels:
      app: ${{ defaults.app_name }}
  template:
    metadata:
      labels:
        app: ${{ defaults.app_name }}
    spec:
      automountServiceAccountToken: false
      initContainers:
      - name: wait-for-clickhouse
        image: busybox:1.36.1
        imagePullPolicy: IfNotPresent
        command: ['sh', '-c', 'until wget -qO- http://${{ defaults.app_name }}-clickhouse-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local:8123/ping | grep Ok; do echo waiting for clickhouse; sleep 2; done;']
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 12Mi
      - name: wait-for-migrations
        image: signoz/signoz-otel-collector:v0.144.2
        imagePullPolicy: IfNotPresent
        command:
          - /bin/sh
          - -c
        args:
          - |
            /signoz-otel-collector migrate sync check &&
            /signoz-otel-collector migrate async check
        env:
          - name: SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_DSN
            value: tcp://${{ defaults.app_name }}-clickhouse-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local:9000
          - name: SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_CLUSTER
            value: cluster
          - name: SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_REPLICATION
            value: "true"
          - name: SIGNOZ_OTEL_COLLECTOR_TIMEOUT
            value: 10m
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 12Mi
      containers:
      - name: ${{ defaults.app_name }}
        image: signoz/signoz:v0.117.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http
        - containerPort: 4320
          name: server
        volumeMounts:
        - name: vn-varvn-libvn-signoz
          mountPath: /var/lib/signoz
        env:
        - name: SIGNOZ_ALERTMANAGER_PROVIDER
          value: signoz
        - name: SIGNOZ_TELEMETRYSTORE_CLICKHOUSE_DSN
          value: tcp://${{ defaults.app_name }}-clickhouse-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local:9000
        - name: SIGNOZ_SQLSTORE_SQLITE_PATH
          value: /var/lib/signoz/signoz.db
        - name: SIGNOZ_PROMETHEUS_ACTIVE__QUERY__TRACKER_PATH
          value: /var/lib/signoz/active-query-log
        - name: SIGNOZ_TOKENIZER_JWT_SECRET
          value: ${{ defaults.tokenizer_jwt_secret }}
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 12Mi
        startupProbe:
          httpGet:
            path: /api/v1/health
            port: 8080
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 36
        livenessProbe:
          httpGet:
            path: /api/v1/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/v1/health?live=1
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
  volumeClaimTemplates:
    - metadata:
        labels:
          app: ${{ defaults.app_name }}
          cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}
        name: vn-varvn-libvn-signoz
        annotations:
          path: /var/lib/signoz
          value: '1'
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi

---
apiVersion: v1
kind: Service
metadata:
  name: ${{ defaults.app_name }}
  labels:
    app: ${{ defaults.app_name }}
    cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}
spec:
  ports:
  - port: 8080
    name: http
  - port: 4320
    name: server
  selector:
    app: ${{ defaults.app_name }}

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${{ defaults.app_name }}-otel-collector
  annotations:
    originImageName: signoz/signoz-otel-collector:v0.144.2
    deploy.cloud.sealos.io/minReplicas: '1'
    deploy.cloud.sealos.io/maxReplicas: '1'
  labels:
    cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}-otel-collector
    app: ${{ defaults.app_name }}-otel-collector
spec:
  replicas: 1
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: ${{ defaults.app_name }}-otel-collector
  template:
    metadata:
      labels:
        app: ${{ defaults.app_name }}-otel-collector
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 0
        seccompProfile:
          type: RuntimeDefault
      initContainers:
      - name: wait-for-clickhouse
        image: busybox:1.36.1
        imagePullPolicy: IfNotPresent
        command: ['sh', '-c', 'until wget -qO- http://${{ defaults.app_name }}-clickhouse-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local:8123/ping | grep Ok; do echo waiting for clickhouse; sleep 2; done;']
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 12Mi
      - name: wait-for-migrations
        image: signoz/signoz-otel-collector:v0.144.2
        imagePullPolicy: IfNotPresent
        command:
          - /bin/sh
          - -c
        args:
          - |
            /signoz-otel-collector migrate sync check &&
            /signoz-otel-collector migrate async check
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        env:
          - name: SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_DSN
            value: tcp://${{ defaults.app_name }}-clickhouse-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local:9000
          - name: SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_CLUSTER
            value: cluster
          - name: SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_REPLICATION
            value: "true"
          - name: SIGNOZ_OTEL_COLLECTOR_TIMEOUT
            value: 10m
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 12Mi
      containers:
      - name: ${{ defaults.app_name }}-otel-collector
        image: signoz/signoz-otel-collector:v0.144.2
        imagePullPolicy: IfNotPresent
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        command:
        - /signoz-otel-collector
        args:
        - --config=/etc/otel-collector-config.yaml
        - --manager-config=/etc/manager-config.yaml
        - --copy-path=/var/tmp/collector-config.yaml
        ports:
        - containerPort: 4317
          name: otlp-grpc
        - containerPort: 4318
          name: otlp-http
        - containerPort: 13133
          name: health
        volumeMounts:
        - name: ${{ defaults.app_name }}-otel-collector-cm
          mountPath: /etc/otel-collector-config.yaml
          subPath: vn-etcvn-otelvn-collectorvn-configvn-yaml
        - name: ${{ defaults.app_name }}-otel-collector-cm
          mountPath: /etc/manager-config.yaml
          subPath: vn-etcvn-managervn-configvn-yaml
        env:
        - name: OTEL_RESOURCE_ATTRIBUTES
          value: host.name=signoz-host,os.type=linux
        - name: LOW_CARDINAL_EXCEPTION_GROUPING
          value: "false"
        - name: SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_DSN
          value: tcp://${{ defaults.app_name }}-clickhouse-0.${{ SEALOS_NAMESPACE }}.svc.cluster.local:9000
        - name: SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_CLUSTER
          value: cluster
        - name: SIGNOZ_OTEL_COLLECTOR_CLICKHOUSE_REPLICATION
          value: "true"
        - name: SIGNOZ_OTEL_COLLECTOR_TIMEOUT
          value: 10m
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 12Mi
        startupProbe:
          httpGet:
            path: /
            port: 13133
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 36
        livenessProbe:
          httpGet:
            path: /
            port: 13133
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 13133
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
      volumes:
      - name: ${{ defaults.app_name }}-otel-collector-cm
        configMap:
          name: ${{ defaults.app_name }}-otel-collector

---
apiVersion: v1
kind: Service
metadata:
  name: ${{ defaults.app_name }}-otel-collector
  labels:
    app: ${{ defaults.app_name }}-otel-collector
    cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}-otel-collector
spec:
  ports:
  - port: 4317
    name: otlp-grpc
  - port: 4318
    name: otlp-http
  selector:
    app: ${{ defaults.app_name }}-otel-collector

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ${{ defaults.app_name }}
  labels:
    cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}
    cloud.sealos.io/app-deploy-manager-domain: ${{ defaults.app_host }}
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-body-size: 32m
    nginx.ingress.kubernetes.io/server-snippet: |
      client_header_buffer_size 64k;
      large_client_header_buffers 4 128k;
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/client-body-buffer-size: 64k
    nginx.ingress.kubernetes.io/proxy-buffer-size: 64k
    nginx.ingress.kubernetes.io/proxy-send-timeout: '300'
    nginx.ingress.kubernetes.io/proxy-read-timeout: '300'
    nginx.ingress.kubernetes.io/configuration-snippet: |
      if ($request_uri ~* \.(js|css|gif|jpe?g|png)) {
        expires 30d;
        add_header Cache-Control "public";
      }
spec:
  rules:
    - host: ${{ defaults.app_host }}.${{ SEALOS_CLOUD_DOMAIN }}
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: ${{ defaults.app_name }}
                port:
                  number: 8080
  tls:
    - hosts:
        - ${{ defaults.app_host }}.${{ SEALOS_CLOUD_DOMAIN }}
      secretName: ${{ SEALOS_CERT_SECRET_NAME }}

---
apiVersion: app.sealos.io/v1
kind: App
metadata:
  name: ${{ defaults.app_name }}
  labels:
    cloud.sealos.io/app-deploy-manager: ${{ defaults.app_name }}
spec:
  data:
    url: https://${{ defaults.app_host }}.${{ SEALOS_CLOUD_DOMAIN }}
  displayType: normal
  icon: "https://raw.githubusercontent.com/labring-actions/templates/kb-0.9/template/signoz/logo.svg"
  name: ${{ defaults.app_name }}
  type: link
