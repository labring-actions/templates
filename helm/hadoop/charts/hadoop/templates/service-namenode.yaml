apiVersion: v1
kind: Service
metadata:
  name: {{ include "hadoop.nameNodeName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "hadoop.nameNodeName" . | quote }}
    cloud.sealos.io/app-deploy-manager: {{ include "hadoop.nameNodeName" . | quote }}
spec:
  type: {{ .Values.hadoop.nameNode.service.type }}
  selector:
    app: {{ include "hadoop.nameNodeName" . | quote }}
  ports:
    - name: rpc
      port: {{ .Values.hadoop.nameNode.service.rpcPort }}
      targetPort: rpc
    - name: http
      port: {{ .Values.hadoop.nameNode.service.httpPort }}
      targetPort: http
