{{- if and .Values.sealos.app.enabled .Values.sealos.ingress.enabled }}
apiVersion: app.sealos.io/v1
kind: App
metadata:
  name: {{ include "hadoop.nameNodeName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "hadoop.nameNodeName" . | quote }}
    cloud.sealos.io/app-deploy-manager: {{ include "hadoop.nameNodeName" . | quote }}
spec:
  data:
    url: https://{{ .Values.sealos.nameNode.hostPrefix }}.{{ .Values.sealos.cloudDomain }}{{ .Values.sealos.nameNode.entryPath }}
  displayType: normal
  icon: {{ .Values.sealos.app.icon | quote }}
  name: "Apache Hadoop HDFS"
  type: link
{{- end }}
