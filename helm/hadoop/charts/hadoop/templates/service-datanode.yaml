apiVersion: v1
kind: Service
metadata:
  name: {{ include "hadoop.dataNodeName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "hadoop.dataNodeName" . | quote }}
    cloud.sealos.io/app-deploy-manager: {{ include "hadoop.dataNodeName" . | quote }}
spec:
  type: {{ .Values.hadoop.dataNode.service.type }}
  selector:
    app: {{ include "hadoop.dataNodeName" . | quote }}
  ports:
    - name: data
      port: {{ .Values.hadoop.dataNode.service.dataPort }}
      targetPort: data
    - name: ipc
      port: {{ .Values.hadoop.dataNode.service.ipcPort }}
      targetPort: ipc
    - name: http
      port: {{ .Values.hadoop.dataNode.service.httpPort }}
      targetPort: http
