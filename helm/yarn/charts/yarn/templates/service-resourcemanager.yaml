apiVersion: v1
kind: Service
metadata:
  name: {{ include "yarn.resourceManagerName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "yarn.resourceManagerName" . | quote }}
    cloud.sealos.io/app-deploy-manager: {{ include "yarn.resourceManagerName" . | quote }}
spec:
  type: {{ .Values.yarn.resourceManager.service.type }}
  selector:
    app: {{ include "yarn.resourceManagerName" . | quote }}
  ports:
    - name: scheduler
      port: {{ .Values.yarn.resourceManager.service.schedulerPort }}
      targetPort: scheduler
    - name: tracker
      port: {{ .Values.yarn.resourceManager.service.resourceTrackerPort }}
      targetPort: tracker
    - name: client
      port: {{ .Values.yarn.resourceManager.service.clientPort }}
      targetPort: client
    - name: admin
      port: {{ .Values.yarn.resourceManager.service.adminPort }}
      targetPort: admin
    - name: http
      port: {{ .Values.yarn.resourceManager.service.webPort }}
      targetPort: http
