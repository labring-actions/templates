{{- if and .Values.sealos.app.enabled .Values.sealos.ingress.enabled }}
apiVersion: app.sealos.io/v1
kind: App
metadata:
  name: {{ include "hdfs.nameNodeName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "hdfs.nameNodeName" . | quote }}
    cloud.sealos.io/app-deploy-manager: {{ include "hdfs.nameNodeName" . | quote }}
spec:
  data:
    url: https://{{ .Values.sealos.hostPrefix }}.{{ .Values.sealos.cloudDomain }}{{ .Values.sealos.app.entryPath }}
  displayType: normal
  icon: {{ .Values.sealos.app.icon | quote }}
  name: "Apache HDFS"
  type: link
{{- end }}
