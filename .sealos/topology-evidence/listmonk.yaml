apiVersion: docker-to-sealos/v1
kind: TopologyEvidence
metadata:
  name: listmonk-topology
spec:
  appName: listmonk
  source: template/listmonk/index.yaml@codex/template-batch-20260804
  resources:
    - kind: Cluster
      name: ${{ defaults.app_name }}-pg
      when: always
      components:
        - name: postgresql
          replicas: 1
    - kind: StatefulSet
      name: ${{ defaults.app_name }}
      when: always
      replicas: 1
