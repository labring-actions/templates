apiVersion: docker-to-sealos/v1
kind: TopologyEvidence
metadata:
  name: wordpress-topology
spec:
  appName: wordpress
  source: template/wordpress/index.yaml@codex/template-batch-20260804
  resources:
    - kind: StatefulSet
      name: ${{ defaults.app_name }}
      when: always
      replicas: 1
    - kind: Cluster
      name: ${{ defaults.app_name }}-mysql
      when: always
      components:
        - name: mysql
          replicas: 1
