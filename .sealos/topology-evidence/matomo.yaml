apiVersion: docker-to-sealos/v1
kind: TopologyEvidence
metadata:
  name: matomo-topology
spec:
  appName: matomo
  source: template/matomo/index.yaml@codex/template-batch-20260804
  resources:
    - kind: Cluster
      name: ${{ defaults.app_name }}-mysql
      when: always
      components:
        - name: mysql
          replicas: 1
    - kind: StatefulSet
      name: ${{ defaults.app_name }}
      when: always
      replicas: 1
