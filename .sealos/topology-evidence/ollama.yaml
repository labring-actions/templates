apiVersion: docker-to-sealos/v1
kind: TopologyEvidence
metadata:
  name: ollama-topology
spec:
  appName: ollama
  source: https://docs.ollama.com/docker@2026-08-04
  resources:
    - kind: StatefulSet
      name: ${{ defaults.app_name }}
      when: always
      replicas: 1
