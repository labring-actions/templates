apiVersion: docker-to-sealos/v1
kind: TopologyEvidence
metadata:
  name: pangolin-topology
spec:
  appName: pangolin
  source: template/pangolin/index.yaml@codex/template-batch-20260804
  resources:
    - kind: Deployment
      name: ${{ defaults.app_name }}-pangolin
      when: always
      replicas: 1
