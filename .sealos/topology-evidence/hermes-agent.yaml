apiVersion: docker-to-sealos/v1
kind: TopologyEvidence
metadata:
  name: hermes-agent-topology
spec:
  appName: hermes-agent
  source: https://raw.githubusercontent.com/NousResearch/hermes-agent/v2026.8.3/docker-compose.yml
  resources:
    - kind: StatefulSet
      name: ${{ defaults.app_name }}
      when: always
      replicas: 1
